#pragma once

#include <wx/wx.h>

namespace ColorCollection {
const wxColour defaultBackground = wxColour(240, 240, 240);

// Red Shades
const wxColour Red_Mandy = wxColour(234, 84, 85);

// Yellow Shades
const wxColour Yellow_Gamboge = wxColour(231, 177, 10);

// Blue Shades
const wxColour Blue_Dodger = wxColour(0, 121, 255);

// Green Shades
const wxColour Green_Bice = wxColour(29, 185, 84);

// Twitter
const wxColour Twitter_Primary = wxColour(29, 161, 242);
const wxColour Twitter_Secondary = wxColour(20, 36, 26);
const wxColour Twitter_DarkGray = wxColour(101, 119, 134);
const wxColour Twitter_LightGray = wxColour(170, 184, 194);
const wxColour Twitter_ExtLightGray = wxColour(225, 232, 237);
const wxColour Twitter_ExtExtLightGray = wxColour(245, 248, 250);
}; // namespace ColorCollection